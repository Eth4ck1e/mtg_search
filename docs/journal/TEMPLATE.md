# <YYYY-MM-DD> — <topic in 4–8 words>

> Copy this file when starting a new entry. Filename pattern: `YYYY-MM-DD-short-topic.md`.

## Context

What was happening when this entry was written. One paragraph. What phase, what was the immediate trigger (a measurement, a bug, a design call, a meeting), what's the goal of the work this entry documents.

## What happened / what was tried

Concrete actions and observations. Code references with `path/to/file.py:line` where relevant. Numbers when they exist (counts, durations, recall@K). Raw enough that someone reading this in 6 months can reconstruct the situation, dense enough that they don't need to.

## Decision

The actual call that was made. One sentence. Optionally a second sentence on scope ("applies to X but not Y").

## Reasoning

Why that call and not the alternatives. List the alternatives that were seriously considered and what tipped the balance. If a decision turns out wrong later, this section is what tells you whether to revisit the criteria or just the conclusion.

## Alternatives considered

- **Option A** — what it was, why not. (Skip the section entirely if there were no serious alternatives.)
- **Option B** — what it was, why not.

## Notes for the final report

Which paper section this feeds (Methodology, Results, Discussion, Future Work). What specific claim or table the entry supports. Quote-ready phrasing is welcome here.

## Open follow-ups

- [ ] Things this entry surfaced that aren't yet resolved. Each item ideally references a phase or a future entry.

## Related

- `docs/roadmap/phase-N-*.md` — link the phase file if this entry advances a sub-task
- `[[other-journal-entry-filename]]` — link prior entries this builds on
